collector:
  kubernetes:
    all:
      incluster: true
      kubeconfigpath: ""
      interval: "60s"
      grpc_timeout: 30

load:
  plugin: snap-plugin-collector-aokubernetes
  task: task-aokubernetes.yaml
